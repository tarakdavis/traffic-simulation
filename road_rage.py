import numpy as np
import matplotlib.pyplot as plt
import statistics


class Car:
    def __init__(self, max_speed=33.333, size=5, accel=2, location=0):
        self.max_speed = max_speed
        self.size = size
        self.speed = speed
        self.location = location

    def is_car_moving(self):
        # will velocity carry you into a car? if yes, False
        slow = randint(0, 9)
        if slow == 0:
            return accel -= 2



class Road:
    def length(self, length=1000):


class Simulation:
